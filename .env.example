DATABASE_URL="postgresql://postgres:postgres@localhost:5433/shortyourlink"

# Observabilidade (Sentry) — TODAS OPCIONAIS. Sem elas o app roda normalmente.
# Servidor (Route Handlers, Server Components):
SENTRY_DSN=
# Browser (Client Components). Inlinado no build: precisa existir em build time.
NEXT_PUBLIC_SENTRY_DSN=
